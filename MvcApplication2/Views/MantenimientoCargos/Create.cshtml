@model MvcApplication2.Models.cargo

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>cargo</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.car_cod)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.car_cod)
            @Html.ValidationMessageFor(model => model.car_cod)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.car_des)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.car_des)
            @Html.ValidationMessageFor(model => model.car_des)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
